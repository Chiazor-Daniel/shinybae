# Shopify Store Configuration
# Replace with your actual Shopify store domain
VITE_SHOPIFY_STORE_DOMAIN=https://your-store.myshopify.com

# Get this from Shopify Admin > Apps > Headless > Create storefront > Copy public access token
# This is SAFE to expose in frontend code - it's designed to be public
VITE_SHOPIFY_PUBLIC_ACCESS_TOKEN=your-public-access-token-here

# Store Localization (optional)
# ISO country code for pricing and shipping
VITE_SHOPIFY_COUNTRY=US

# Language code for content
VITE_SHOPIFY_LANGUAGE=en

# Development Settings
# Set to true to use Shopify checkout, false to use your custom checkout
VITE_USE_SHOPIFY_CHECKOUT=true

# Optional: For testing different modes
VITE_DEVELOPMENT_MODE=true

# Optional: Analytics and tracking (if needed later)
VITE_GA_TRACKING_ID=
VITE_SHOPIFY_SHOP_ID=

# Environment
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Shopify store details
# 3. Never commit .env to git (it's already in .gitignore)
# 4. The VITE_ prefix makes these variables available in your React app
